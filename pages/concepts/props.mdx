# Props

Props are the data pass to a component as its attributes.

```js {2} copy
return `
  <Audio id="audio" song="${stringify(song)}" />
`;
```

The song passed to the `Audio` component as one of its attributes is a prop. 

