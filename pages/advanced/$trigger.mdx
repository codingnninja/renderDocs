# $trigger utility

$trigger is used to call an action when user interact with certain elements. That means you can use it to trigger an action when an interaction like click, hover and so on happens.
